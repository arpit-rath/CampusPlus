[pytest]
# `asyncio_mode = auto` would also work, but being explicit keeps the pure
# tests (which call asyncio.run themselves) independent of this setting.
asyncio_mode = strict
asyncio_default_fixture_loop_scope = session
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
