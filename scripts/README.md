# scripts

- `seed_demo.py` — generates realistic, semantically-clustered fake
  complaints (several near-duplicate reports in one building, plus a few
  genuinely distinct issues) against the running API. Doubles as the test
  fixture for the clustering logic and as the data behind the "chaos
  button" demo feature. See `docs/build-plan.md` §10.3.
