# shared-types

Placeholder for OpenAPI-generated TypeScript types once the FastAPI schema
stabilizes (`openapi-typescript http://localhost:8000/openapi.json -o index.ts`).
Until then, `apps/web/src/lib/api.ts` hand-maintains the types it needs —
don't block on this during the hackathon.
